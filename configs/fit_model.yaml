# @package _global_

defaults:
  - _self_
  - local: default
  - model: default

input_dir: ${local.data_dir}/generated_data/
output_dir: ${local.data_dir}/model/

path_to_measurement_model: ${local.data_dir}/model/gt_mes_model.npz



